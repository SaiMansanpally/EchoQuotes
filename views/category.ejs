<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= category %> Quotes</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Orbitron&display=swap" rel="stylesheet">
</head>
<body>
    <header>
        <h1>Best Quotes</h1>
        <select id="categorySelect" onchange="window.location.href = this.value;">
            <option value="/category/Life" <%= category === 'Life' ? 'selected' : '' %>>Life</option>
            <option value="/category/Smile" <%= category === 'Smile' ? 'selected' : '' %>>Smile</option>
            <option value="/category/Happy" <%= category === 'Happy' ? 'selected' : '' %>>Happy</option>
            <option value="/category/Sad" <%= category === 'Sad' ? 'selected' : '' %>>Sad</option>
            <option value="/category/Travel" <%= category === 'Travel' ? 'selected' : '' %>>Travel</option>
            <option value="/category/Inspire" <%= category === 'Inspire' ? 'selected' : '' %>>Inspire</option>
        </select>
    </header>
    <main>
        <div class="quotes-grid">
            <% quotes.forEach(quote => { %>
                <div class="quote-tile">
                    <p class="quote-text"><%= quote.text %></p>
                    <p class="quote-author">- <%= quote.author %></p>
                </div>
            <% }) %>
        </div>
    </main>
    <script>
        // Optional: Handle dropdown styling or behavior if needed
    </script>
</body>
</html>